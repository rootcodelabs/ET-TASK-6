<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    
    <!-- Console appender with masking -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ee.buerokratt.xtr.configuration.LogMaskingPatternLayout">
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSS'Z', UTC} %5p %c{1} - %m%n</pattern>
        </layout>
    </appender>
    
    <!-- File appender with masking (if you want to log to file) -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/var/log/xtr/application.log</file>
        <layout class="ee.buerokratt.xtr.configuration.LogMaskingPatternLayout">
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSS'Z', UTC} %5p %c{1} - %m%n</pattern>
        </layout>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>/var/log/xtr/application-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Set logging level for XTR application -->
    <logger name="ee.buerokratt.xtr" level="INFO"/>
    
    <!-- Reduce verbose Spring Boot logging -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.springframework.boot" level="INFO"/>
    
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <!-- Uncomment to enable file logging -->
        <!-- <appender-ref ref="FILE"/> -->
    </root>
</configuration>
